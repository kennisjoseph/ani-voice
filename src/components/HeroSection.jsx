import React from 'react'

const HeroSection = () => {
  return (
    <div>
      {/* requirements: fonts, middle data(that explains your proj), button(getting started) */}
    </div>
  )
}

export default HeroSection
